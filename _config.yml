title: TickerStrike
tagline: Put your trading ideas on trial. Then strike.
description: >-
  TickerStrike puts your trading setups on trial — testing whether they carry a
  real, measurable edge against fair baselines and out-of-sample data — so you
  can determine when and where is the right place to strike using a targeted
  plan with surgical precision.
url: "https://tickerstrike.com"
baseurl: ""

markdown: kramdown
sass:
  style: compressed

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - vendor
